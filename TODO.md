# TODO:
* Figure out "Setting initial properties failed" in system log when opening config page
* Figure out why console debug log is on by default
* Implement symbols selection from alternatives list on config page
